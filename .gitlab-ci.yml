cache:
  key: main-composer
  paths:
    - .cache

stages:
  - composer
  - unit
  - acceptance
  - functional

composer:
  image: ubuntu-20.04
  stage: composer
  script:
    - Build/Scripts/runTests.sh -s composerValidate -p 7.4
    - Build/Scripts/runTests.sh -s composerInstall -p 7.4
